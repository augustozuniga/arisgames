package org.arisgames.editor.view
{
	import org.arisgames.editor.model.GameObjectInstance;
	import org.arisgames.editor.controller.Controller;

	public class GPSMarker extends MarkerIcon
	{
		public function GPSMarker(instance:GameObjectInstance, currentController:Controller)
		{
			super(instance, currentController);
		}
		
	}
}